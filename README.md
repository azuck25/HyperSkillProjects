This project is a simple banking system which generates unique credit card numbers using the Luhn algorithm to calculate the check sum digit of the card number. The cards are indexed into a dictionary currently and accessed in O(n) time by using dictionary indexing to access account data. This script sets the platform as a proof of concept to build more advanced accounting systems.
